# Modules
import pandas as pd

# Other files
from helpers import *

class DecisionTreeClassifier:
    """
    Decision Tree Classifier model.
    """

    def __init__(self) -> None:
        pass

    def predict(self, data: pd.DataFrame) -> list:
        """
        Given a pd.DataFrame with the same columns as the training data,
        return a list where the value at each index is the predicted classification 
        of that entry in <data>.
        """
        pass